/**
 * Coffee module-specific types. The heavy lifting lives in `./data.ts`
 * which exports `Recipe`, `RecipeCategory`, and friends.
 */

export type { Difficulty, Ingredient, Recipe, RecipeCategory } from './data';
