# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vite
vite.config.ts.timestamp-*.mjs

# Editor directories and files
.vscode/*
!.vscode/extensions.json

# Claude Code local agent config (machine-specific; may contain local secrets)
.claude/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables (SECURITY - Never commit API keys)
.env
.env.local
.env.development
.env.production
.env.*.local
.vercel

# Convex
# `convex/_generated` IS committed (hand-written stubs let the app build before
# `npx convex dev` runs; that command regenerates them in place).
.env*.local
.env.temp
backups/
